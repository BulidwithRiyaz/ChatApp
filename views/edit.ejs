<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Chat</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
      background: #f4f6f9;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .card {
      background: #fff;
      padding: 25px 30px;
      border-radius: 12px;
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.1);
      width: 400px;
    }

    h1 {
      text-align: center;
      margin-bottom: 15px;
      color: #2c3e50;
      font-size: 22px;
    }

    p {
      font-size: 14px;
      margin-bottom: 10px;
      color: #555;
    }

    textarea {
      width: 100%;
      padding: 12px;
      margin: 15px 0;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 14px;
      resize: vertical;
      transition: border 0.3s ease;
      min-height: 120px;
    }

    textarea:focus {
      border-color: #3498db;
      outline: none;
    }

    button {
      width: 100%;
      padding: 12px;
      background: #3498db;
      color: white;
      font-size: 15px;
      font-weight: bold;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      transition: background 0.3s ease;
    }

    button:hover {
      background: #2d83c5;
    }

    .info {
      background: #f0f4f9;
      padding: 10px;
      border-radius: 6px;
      margin-bottom: 15px;
      font-size: 13px;
      color: #333;
    }
  </style>
</head>
<body>
  <div class="card">
    <h1>Edit Your Message</h1>
    <div class="info">
      <p><strong>Id:</strong> <%= chat._id %></p>
      <p>Editing message of <strong><%= chat.from %></strong> sending to <strong><%= chat.to %></strong></p>
    </div>
    <form method="POST" action="/chats/<%= chat._id %>?_method=PUT">
      <textarea name="msg" placeholder="Enter your message here"><%= chat.msg %></textarea>
      <button>Edit Chat</button>
    </form>
  </div>
</body>
</html>
